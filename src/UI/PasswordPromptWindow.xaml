<Window x:Class="UI.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YJSCGAURD" Height="180" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#2D2D30">
    <Grid Margin="15">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="작업을 수행하려면 비밀번호를 입력하세요." Foreground="White" Margin="0,0,0,10"/>
            <PasswordBox x:Name="pwdBox" Height="25" Margin="0,0,0,15" KeyDown="pwdBox_KeyDown"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="확인" Width="70" Height="25" Margin="0,0,10,0" Click="btnConfirm_Click"/>
                <Button Content="취소" Width="70" Height="25" Click="btnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
